# Experimental Setup

Reserved for experimental configuration and setup files. For SS2, train/test split logic lives inline in `src/models/model1_logistic_regression.ipynb` and `src/models/model2_xgboost.ipynb` (see each notebook's "Configuration and Data Load" / "Time-Based Train/Test Split" sections), rather than as separate files here.