# Visualisation

Reserved for charting and visualisation scripts. For SS2, all charts are produced inline within `src/preprocessing/preprocessing.ipynb`, `src/features/feature_engineering.ipynb`, and `src/models/model1_logistic_regression.ipynb`/`model2_xgboost.ipynb`, and saved to `reports/`, rather than as separate scripts here.
